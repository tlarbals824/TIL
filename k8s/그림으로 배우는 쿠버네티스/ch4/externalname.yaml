apiVersion: apps/v1
kind: Deployment
metadata:
  name: dp-loadbalancer
  labels:
    app: dp-loadbalancer
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dp-loadbalancer
  template:
    metadata:
      labels:
        app: dp-loadbalancer
    spec:
      containers:
      - name: nginx
        image: docker.io/library/nginx
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: svc-loadbalancer
spec:
  type: LoadBalancer
  selector:
    app: dp-loadbalancer
  ports:
  - protocol: TCP
    port: 80            # 서비스 포트
    targetPort: 80      # Pod의 컨테이너 포트
  
